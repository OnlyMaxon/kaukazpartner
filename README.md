# Kaukaz Partner - Сайт для водителей и курьеров

Современный лендинг для компании Kaukaz Partner, предоставляющей услуги трудоустройства водителей (Uber, Bolt, FreeNow) и курьеров (Wolt, Glovo) в Варшаве.

## 🚀 Технологии

- **React 18** + **TypeScript**
- **Vite** - быстрая сборка
- **Tailwind CSS** - стилизация
- **Lucide React** - иконки

## 📁 Структура проекта

```
src/
├── components/          # React компоненты
├── context/            # Контекст локализации
├── data/               # JSON данные (легко редактируемые)
│   ├── config.json     # Контакты, настройки компании
│   ├── faq.json        # FAQ на 3 языках
│   ├── reviews.json    # Отзывы на 3 языках
│   └── blog.json       # Блог на 3 языках
└── locales/            # Переводы интерфейса
    ├── en.json         # Английский
    ├── pl.json         # Польский
    └── ru.json         # Русский
```

## ⚙️ Настройка

### 1. Установка зависимостей
```bash
npm install
```

### 2. Запуск в режиме разработки
```bash
npm run dev
```

### 3. Сборка для продакшена
```bash
npm run build
```

## 📝 Как обновлять контент

### Контактные данные
Отредактируйте файл `src/data/config.json`:
```json
{
  "company": {
    "email": "ваш@email.pl",
    "phone": "+48 123 456 789",
    "whatsapp": "+48123456789",
    "telegram": "ваш_telegram"
  }
}
```

### Отзывы
Отредактируйте файл `src/data/reviews.json` - добавьте/измените отзывы на каждом языке.

### FAQ
Отредактируйте файл `src/data/faq.json` - добавьте/измените вопросы на каждом языке.

### Блог
Отредактируйте файл `src/data/blog.json` - добавьте/измените статьи на каждом языке.

### Тексты интерфейса
Отредактируйте файлы в папке `src/locales/`:
- `en.json` - английский
- `pl.json` - польский  
- `ru.json` - русский

## 📧 Настройка формы контактов

Форма использует [Web3Forms](https://web3forms.com/) (бесплатно, до 250 заявок/месяц):

1. Зарегистрируйтесь на https://web3forms.com/
2. Получите Access Key
3. Замените `YOUR_WEB3FORMS_KEY` в файле `src/components/Contacts.tsx`

Если Web3Forms недоступен, форма автоматически откроет email-клиент.

## 🌐 Деплой на Cloudflare Pages

### Первоначальная настройка:

1. Загрузите проект на GitHub
2. Войдите в [Cloudflare Dashboard](https://dash.cloudflare.com/)
3. Перейдите в **Pages** → **Create a project** → **Connect to Git**
4. Выберите репозиторий `kaukazpartner`
5. Настройки сборки:
   - **Framework preset**: `Vite`
   - **Build command**: `npm run build`
   - **Build output directory**: `dist`
6. Нажмите **Save and Deploy**

### Автоматическое обновление:

После настройки каждый `git push` в `main` автоматически обновит сайт:

```bash
git add .
git commit -m "Обновление контента"
git push
```

### Собственный домен:

1. В Cloudflare Pages → ваш проект → **Custom domains**
2. Добавьте домен `kaukazpartner.pl`
3. Настройте DNS записи как указано

## 🔧 Дополнительные настройки

### SEO
Отредактируйте `index.html` для изменения:
- Заголовка сайта
- Описания (meta description)
- OG-изображения (создайте `public/og-image.png` размером 1200x630px)

### Цвета
Измените цветовую схему в `tailwind.config.js`:
```js
colors: {
  'kaukaz-accent': '#06ACC4', // Основной акцентный цвет
}
```

## 📱 Локализация

Сайт поддерживает 3 языка:
- 🇬🇧 Английский (по умолчанию)
- 🇵🇱 Польский
- 🇷🇺 Русский

Язык определяется автоматически по настройкам браузера и сохраняется в localStorage.

## 📄 Лицензия

Проприетарная лицензия - Kaukaz Partner © 2026
